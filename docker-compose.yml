version: '3.8'

services:
  # Infrastructure
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: vrip
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  # MCP Layer
  mcp-gateway:
    build:
      context: .
      dockerfile: services/mcp-gateway/Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - mcp-postgres
      - mcp-qdrant

  mcp-postgres:
    build:
      context: .
      dockerfile: services/mcp-postgres/Dockerfile
    env_file: .env
    depends_on:
      - postgres

  mcp-qdrant:
    build:
      context: .
      dockerfile: services/mcp-qdrant/Dockerfile
    env_file: .env
    depends_on:
      - qdrant

  # Intelligence Layer
  agent-service:
    build:
      context: .
      dockerfile: services/agent-service/Dockerfile
    env_file: .env
    ports:
      - "8001:8001"
    depends_on:
      - mcp-gateway

  # Public API
  api-gateway:
    build:
      context: .
      dockerfile: services/api-gateway/Dockerfile
    ports:
      - "8080:8080"
    env_file: .env
    depends_on:
      - agent-service

volumes:
  postgres_data:
  qdrant_data:
